import React from 'react'
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

const Notification: React.FC = () => {
  return null // Notification is handled in NotificationContext
}

export default Notification